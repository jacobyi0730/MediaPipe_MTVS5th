[project]
name = "mediapipe-hand-webcam"
version = "0.1.0"
description = "Webcam hand tracking demo with MediaPipe"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mediapipe>=0.10.14",
    "opencv-python>=4.10.0.84",
]

[tool.uv]
package = false
